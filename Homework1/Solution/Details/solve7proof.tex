
    \clearpage
    $2^{2^{n+1}}$\\
    $2^{2^n}$\\

    $(n+1)!$\\
    $n!$\\

    $e^n$\\
    $n \cdot 2^n$\\
    $2^n$\\
    $\frac{3}{2}^{n}$\\

    $(\lg n)^{\lg n}$ \\ %$x=\lg n, then (\lg n)^{\lg n} = x^x < (k^{2^{x/2}})^{2^{x/2}} = k^{2^x} = k^n (k>1)$
    $n^{\lg\lg n}$\\ %$n = 2^x$ 换元，可证$=(\lg n)^{\lg n}$

    $(\lg n)!$\\ % $n = 2^x$ 换元可证比幂函数快，指数函数慢

    $n^3$\\
    $n^2$, $4^{\lg n}$\\ % $n^2$
    $n\lg n$\\
    $\lg (n!)$\\ % $  n < lg(2^n) < \lg (n!) = \sum \lg i < n \lg n$
    $n$, $2^{\lg n}$\\ % $n$
    $(\sqrt{2})^{\lg n}$\\ % $n^{1/2}$

    $2^{\sqrt{2\lg n}}$\\
    % $2^{\sqrt{2\lg n}} = (2^{\lg n})^{\sqrt{2/\lg(n)}} = n^{\sqrt{2/\lg(n)}}< n^k$
    %令 $x=\lg n$，欲证$2^{\sqrt{2x}}>x^2$：$\forall k>1,\sqrt x > \log_k x \rightarrow k^{\sqrt x} > x \rightarrow (2^{\sqrt 2 /2})^{\sqrt x} > x \rightarrow 2^{\sqrt{2x}}>x^2$

    $\lg^2 n$\\
    % \lg n 
    $\ln n$\\

    $\sqrt{\lg n}$\\ % $x=\lg n, then \ln\ln n = \ln \ln {2^x} = \ln {xln2} = \ln x + \ln \ln 2 < \sqrt{x} < x \ln 2 < \ln {2^x} = \ln n$
    $\ln\ln n$\\

    $2^{\lg^* n}$\\ % $ = (\lg^* n) + 1$
    $\lg^* n$\\
    $\lg^*(\lg n)$\\ % $ = (\lg^* n) - 1$

    $\lg(\lg^* n)$\\

    $n^{1/\lg(n)}$\\ %换元可证$ = 2$
    $1$\\
